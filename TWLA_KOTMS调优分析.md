# KOTMS 调优能否提升量化精度？——实验分析报告

> 实验日期：2026-08-19
> 实验对象：Qwen3-1.7B q_proj（2048×2048，l=8, r=256），8 个 wikitext 样本，单层量化 MSE 对比
> 背景：4B 旋转 checkpoint 三模态中心峰占比 0.41（理想 0.33），曾推断"旋转不完美 → 调参可提升"

---

## 一、实验设置

对同一层原始权重分别训练 KOTMS 旋转（不同超参），然后统一用 E2M-ATQ + TRA-GPTQ（U 语义 fp64 版）量化，比较每样本输出 MSE。所有配置的参考输出一致（已验证旋转等价性：max rel diff 5.8e-4）。

## 二、结果

### 2.1 gmm_iters 扫描（default sigma0=0.25, sigma1=0.25, balance_w=0.1, lr=1e-2）

| 配置 | GMM NLL（收敛值） | 三模态 zero_frac | **量化 MSE** |
|---|---|---|---|
| 不旋转（直接量化） | — | 0.456 | **525.9** |
| iters=100 | -0.396 | 0.4058 | **374.4** |
| iters=300 | -0.415 | 0.4040 | **371.4** |
| iters=600 | ~-0.42 | 0.4033 | **383.5** |
| sherry checkpoint（现有） | — | ~0.41 | **385.1** |

**关键发现**：
1. **旋转 vs 不旋转：525.9 → 374.4（-29%）**——KOTMS 旋转大幅改善量化精度，收益真实
2. **iters 100→600：GMM NLL 继续下降 6%（-0.396→-0.42），但量化 MSE 完全无改善**（374/371/384，噪声级）
3. 现有 sherry checkpoint（385）与我们重新训练的 100 iters（374）相当——**现有旋转质量不差，无需重跑**

### 2.2 sigma/balance 敏感性（iters=200）

| 配置 | zero_frac |
|---|---|
| default (0.25/0.25/0.1) | **0.4044** |
| sharp (0.15/0.20) | 0.4094（更差） |
| sharp2 (0.10/0.15) | 0.4121（更差） |
| balw=0.3 (0.20/0.20) | 0.4057（无变化） |
| wide (0.35/0.35) | 0.4051（无变化） |

**默认参数已接近最优**，调 sigma 反而让三模态质量变差。

## 三、结论

### 常规 KOTMS 调优（iters / sigma / balance / lr）不能显著提升量化精度

**根因：目标函数错配**。KOTMS 优化的是"三高斯混合似然（GMM NLL）"，而最终精度取决于"三值化残差（E2M 交替优化的输出）"：
- 一旦分布接近三模态（zero_frac ≈ 0.40），继续优化 NLL 不再降低三值化残差（实验 2.1 直接证明）
- zero_frac 0.40 可能已是该目标下的平台期；且 zero_frac 与 MSE 的相关性本身有限
- sigma 调优把中心峰调"尖"反而增加 zero_frac（更胖），与直觉相反

### 旋转的收益已经拿到

-29% 的 MSE 改善（不旋转→旋转）在当前 4B 模型上已经实现（KOTMS 阶段正常执行，sherry 或我们训练的旋转质量相当）

### 如果要进一步突破（新方法方向，超出调参）

1. **端到端旋转目标**：把 GMM 损失替换/混合为"可微三值化损失"（如 soft 三值 + 重建误差），让旋转直接最小化量化残差——这是论文之外的新方法
2. **逐层自适应 sigma**：不同层的最优 sigma 可能不同（当前全局固定）
3. **旋转与 E2M 联合优化**：交替优化旋转与三值参数

## 四、对后续实验的指导

- **无需重跑 4B KOTMS**（现有旋转质量已达标）
- 精度提升应聚焦已验证的方向：percdamp 调优（单层 MSE -10~20%）、nsamples 128（Hessian 更准）、以及量化后蒸馏微调
