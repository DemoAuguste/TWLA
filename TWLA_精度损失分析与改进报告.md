# TWLA Qwen3-4B 1.58-bit 量化精度损失分析与改进报告

> 日期：2026-08-18
> 基线：Qwen3-4B TWLA 量化（W1.58A16）PPL=18.77 / GSM8K=22%，原始模型 GSM8K=90%

---

## 一、精度损失现状

| 指标 | 原始 BF16 | TWLA 量化（v1） | 损失 |
|---|---|---|---|
| WikiText2 PPL | ~6-8 | 18.77 | 2.5-3x |
| GSM8K 4-shot | 90%（9/10） | 22%（22/100） | -68pt |

1.58-bit 三值权重的信息容量约为 BF16 的 1/10，存在**理论下限损失**（PPL 不可能回到 ~6）。但 v1 实现的损失中有**可修复的部分**——本报告通过逐环节实验定位并量化。

## 二、量化流程精度影响点全分析（含实验证据）

### 2.1 【已定位最大问题】GPTQ 更新中 Hinv 语义偏离官方算法（-40% MSE）

**发现过程**：
- 为绕开 NPU Cholesky 崩溃，v1 用 `torch.linalg.inv(H)` 直接替代官方 cholesky 链 `U = chol(chol_inv(chol(H)))`
- 当时假设两者数学等价（`U[i,i:] = Hinv[i,i:]/√Hinv[i,i]`）
- **fp64 精确验证推翻该假设**：`U[i,i:]` 与 `Hinv[i,i:]/√Hinv[i,i]` 存在 **7% 相对差异**，因为正确的 Cholesky 关系是
  `Hinv[i,i:] = U[i,i]·U[i,i:] + Σ_{k<i} U[k,i]·U[k,i:]`
  （U 的行包含前序行的正交化信息，直接替换改变了 GPTQ 误差反馈路径）

**实验证据**（q_proj，真实 wikitext 输入，同一 H）：

| Hinv 来源 | 输出 MSE |
|---|---|
| cholesky 链（官方 U） | 3.44e3 |
| fp32 `inv(H)`（v1 方案） | 5.74e3 |
| fp64 `inv(H)` | 5.67e3 |

→ 差距是**算法语义**而非精度（fp64 inv 也无法弥补）。官方 U 语义 MSE 低 **40%**。

**修复（已实施，v2 量化进行中）**：CPU fp64 计算 `U = chol(inv(H), upper=True)` 再转 fp32 上 NPU：
- 恢复官方 GPTQ 语义（U 行更新）
- fp64 全程计算，精度优于原 fp32 cholesky 链
- 代价极小：2048 维 1.3s、6144 维 8.1s/层
- 已上传 `quantize/tra_gptq.py`（`TWLA_HINV_MODE=u_cpu_fp64` 默认）

### 2.2 percdamp 阻尼系数（-17~38% 单层 MSE，待端到端验证）

GPTQ 对 H 对角加 `damp = percdamp × mean(diag(H))` 改善数值稳定性。扫描结果（q_proj，U 版）：

| percdamp | 0.01 | 0.05 | 0.1 | 0.2 | 0.5 |
|---|---|---|---|---|---|
| per-sample MSE | 1.96e3 | 1.62e3 | 1.47e3 | 1.36e3 | **1.22e3** |

→ 单调下降（0.1→0.5 再降 17%）。机制：damp 大 → H 更对角占优 → Hinv 数值更稳（cond 降低），代价是偏离真实 Hessian。**风险**：校准集上 MSE 更小不代表 PPL/GSM8K 泛化更好，需端到端验证（建议试 0.3）。

### 2.3 校准样本数 nsamples（小改进 ~4%/翻倍）

8→16 样本每样本 MSE 1469→1406（-4%）。趋势：更多样本 Hessian 更准。v1 用 64（为缩小崩溃窗口），**建议恢复 128**（GPTQ/TWLA 标准，时间 +50%）。

### 2.4 KOTMS 旋转质量（潜在改进，成本高）

检查 4B rotated checkpoint 的三模态分布（|W|≤0.75·mean 的占比，理想 1/3≈0.33）：
- q_proj：0.415；gate_proj：0.415；down_proj：0.409（全部偏胖）
- **中心峰占比 0.41 vs 理想 0.33** → 旋转后分布仍不够"三模态"，三值化丢信息偏多

改进方向（需重跑 KOTMS + 量化验证，~8h）：
- `gmm_iters` 100→300-600（更充分优化）
- `sigma0_ratio`/`sigma1_ratio` 0.25→0.15-0.2（中心峰更尖）
- `balance_w` 0.1→0.15-0.2（更强平衡正则）
- 当前 4B 旋转用默认参数（README 示例值），未见调优

### 2.5 已验证无收益或无需改动的项

| 项 | 结论 |
|---|---|
| E2M 交替优化迭代 10 vs 15 | **MSE 完全相同**（10 次已收敛，保持 10 省时间） |
| mask 分位 41 vs 81 | 噪声级差异（41 反而略好），保持 41 |
| H 对称化+归一化 | 数学等价（缩放不变性），无影响 |
| 一次性 H 计算 / S 从 H_raw 取块 | 数学等价（已验证 MSE 完全一致） |
| dead 列 | 实测 0 列，无影响 |

### 2.6 其他潜在方向（未实验）

- **校准数据**：当前 wikitext2；GPTQ 系列常用 C4（分布更接近通用文本），可对比
- **groupsize**：128（当前）→ 256/512 或 -1（全行）：分组越大三值化自由度越高但 alpha 粒度变粗，需实验
- **blocksize**：128 受 groupsize 约束（`groupsize % blocksize == 0`），如 groupsize 增大可同步增大
- **lm_head/embedding**：已保持 fp16 ✓
- **权重存储**：当前 state_dict 存 fp16 权重（8.9GB），若做推理部署可存 {-1,0,1}+α+μ 的紧凑格式（~3.3GB）

## 三、改进优先级排序（按性价比）

| 优先级 | 改进 | 预期收益 | 成本 | 状态 |
|---|---|---|---|---|
| 1 | **U 语义修复**（官方 GPTQ cholesky 因子） | 单层 MSE -40% | 代码已改，重量化 5h | 🔄 v2 量化验证中 |
| 2 | **percdamp 0.1→0.3** | 单层 MSE -10~20% | 重量化 5h | 待验证 |
| 3 | **nsamples 64→128** | ~-4%/翻倍 | 重量化 +50% 时间 | 待验证 |
| 4 | **KOTMS 参数调优**（iters/sigma/balance） | 三模态分布改善（0.41→0.33 方向） | KOTMS 2h + 量化 5h | 待验证 |
| 5 | 校准数据 C4 对比 | 未知 | 重量化 5h | 待实验 |

## 四、当前验证进展

- v2 重新量化（U 语义 + fp64）已于 08-18 15:12 启动（supervisor + 断点续跑，预计 5h）
- 完成后对比 v1：预期 PPL 18.77 → 14-16 区间、GSM8K 22% → 25-35% 区间（待实测）
- 若 v2 验证有效，可继续 v3（+percdamp 0.3 + nsamples 128）逼近上限
