# 两卡 DDP 训练方案分析

> 目标：将 EdgeRazor QAT+KD 训练从单卡扩展到 **2 卡数据并行（DDP）**，扩大有效 batch，缓解"蒸馏坍缩"（ARC 25% 退化）。

---

## 一、为什么需要两卡

当前单卡训练的**核心瓶颈是有效 batch 过小**：

| | 官方 Qwen3-1.7B | 当前单卡 |
|---|---|---|
| 有效 batch | 1536（12×16×8卡） | 128（4×32×1卡） |
| 与官方差距 | — | **12 倍** |

三值量化（STE）在高 KD 权重（KL/task=20:1）下，小 batch 导致梯度噪声大，student 坍缩到"输出无信息先验"（ARC 从 33% 跌到 25%，pred 97% 偏 B）。

**两卡 DDP 直接翻倍有效 batch（128→256），且不增加单卡显存压力。**

---

## 二、环境可行性（已逐项验证 ✅）

| 检查项 | 结果 |
|---|---|
| HCCL 后端 | ✅ `torch.distributed.Backend.HCCL = 'hccl'` |
| torchrun | ✅ `/root/miniconda3/envs/agent/bin/torchrun` |
| torch_npu 2.10 | ✅ 支持 `torch.npu` + 8 卡可见 |
| accelerate 1.14 | ✅ `is_npu_available()` 支持 torch_npu |
| transformers 5.14 | ✅ Trainer 自动 DDP 包装 |
| 空闲 NPU | ✅ NPU 0-6 全部空闲（仅 7 被当前训练占用） |

---

## 三、关键技术方案

### 3.1 启动方式

用 `torchrun` 启动，HCCL 后端自动初始化：

```bash
export ASCEND_RT_VISIBLE_DEVICES=0,1   # 物理卡 0 和 1（或 1,2 避开冲突）
torchrun --nproc_per_node=2 \
  erazor_train_npu_ddp.py \
  --data_dir /dev/shm/erazor_data \
  --output_dir /opt/.../edgerazor_qwen3_output_2card \
  ...
```

### 3.2 设备映射（核心改动）

单卡脚本硬编码 `npu:0`，需改为按 `LOCAL_RANK` 映射：

```python
import os
local_rank = int(os.environ.get("LOCAL_RANK", 0))
device = f"npu:{local_rank}"
torch_npu.set_device(local_rank)  # 关键：设置当前 device
student.to(device)
teacher.to(device)
```

### 3.3 数据并行

- `transformers.Trainer` 在检测到 `world_size > 1` 时**自动用 DDP 包装 model**（不可用 `is_loaded_in_8bit/4bit`）。
- 需要设置 `TrainingArguments(ddp_find_unused_parameters=False)`（官方也用了，因为 gradient checkpointing 有未用参数）。
- teacher 不参与 DDP（每卡独立 forward，已冻结）。

### 3.4 batch 选择（三个方案）

| 方案 | 每卡 bs | grad_acc | 有效 batch | 单卡显存 | 优势 |
|---|---|---|---|---|---|
| **A（推荐）** | 4 | 32 | **256** | 58GB（不变） | batch 翻倍，最稳 |
| B | 4 | 16 | 128 | 58GB | 每步快 2 倍，但 batch 不涨 |
| C | 6 | 32 | **384** | ~62GB（风险） | batch 更大，但可能 OOM |

**推荐方案 A**：每卡 bs=4×grad_acc=32，有效 batch 256。虽然仍远小于官方 1536，但翻倍后梯度更稳，且不冒 OOM 风险。

### 3.5 断点续训（关键）

当前训练已到 1304 步，浪费 19h 不划算。用最近 checkpoint 续训：

```python
# Trainer 自动从 output_dir 找 checkpoint-*，传入 resume_from_checkpoint
trainer.train(resume_from_checkpoint=True)  # 自动找最新
```

两卡续训的 checkpoint 兼容性：
- checkpoint 里的 `model.safetensors` 是完整权重（QLinear float），DDP 下加载无差异。
- optimizer/scheduler state 也可加载（AdamW 状态），LR 会从断点继续。

**注意**：checkpoint 需要能兼容两卡。transformers 的 DDP checkpoint 保存是 rank 0 聚合的，所以 `checkpoint-1000`（单卡训）可以直接被两卡加载。

---

## 四、实施步骤

1. **等当前训练出 checkpoint-1500**（约 3 小时后，当前 1304 步）作为最热的续训点
   —— 或直接停掉，用 checkpoint-1000。
2. **写 `erazor_train_npu_ddp.py`**（基于现有脚本改造，核心改动 3 处）：
   - `local_rank` 设备映射
   - `torch_npu.set_device(local_rank)`
   - `TrainingArguments(ddp_find_unused_parameters=False)`
3. **改 runner 脚本**：`torchrun --nproc_per_node=2` 启动，`ASCEND_RT_VISIBLE_DEVICES=0,1`。
4. **冒烟测试**（60 步）验证两卡通信、loss 正常、batch=256 生效。
5. **正式启动**，从 checkpoint 续训，跑满剩余 ~2000 步。

---

## 五、风险与对策

| 风险 | 对策 |
|---|---|
| 两卡显存超限 | 方案 A 每卡 bs=4 不变，实测单卡 58GB 有 ~6GB 余量 |
| HCCL 通信失败 | 冒烟阶段先跑 `torch.distributed.all_reduce` 测试 |
| checkpoint 不兼容 | 冒烟阶段验证 `resume_from_checkpoint` 加载成功 |
| 自治重启循环需改 | runner 脚本改 torchrun 启动，SIGKILL 后 torchrun 自动重启每个 rank |

---

## 六、结论

**推荐方案 A：停掉当前单卡训练 → 用最新 checkpoint → 两卡 DDP 续训，有效 batch 256。**

- 损失：19h 已花，但从 checkpoint 续训**不损失**训练进度（相当于换引擎继续跑）。
- 收益：batch 翻倍 → 梯度更稳 → 大概率缓解蒸馏坍缩 → ARC 有望恢复到健康的爬坡轨迹。

**关键待确认**：现在停（用 checkpoint-1000，损失 300+ 步）还是等 checkpoint-1500（再等 ~3h，损失更少）？